# DECISIONS.md

**Append only. Never edit a line. Never write prose.**

One line per verdict:

```
- DD/MM · AXIS · WINNER · short reason
```

- **AXIS** — the axis judged. `LOOP` for verdicts about the method.
- **WINNER** — what was decided, not what was discussed.
- **reason** — one clause. If it needs a paragraph, the paragraph belongs to
  `TASTE-LOOP-LEARNING.md` and this line points there.

Does not go here: anything that changes (that is state, it has an owner in a state file);
backlog; a superseded line deleted.

When a verdict here supersedes what a **state** file declares, fix that file in the same
turn — `TASTE-LOOP.md` §3.8. Recording is not propagating.

---

- 14/08 · LOOP · intake complete · axes silhouette/value, animation weight; animated-impostor gate; taste-loop v0.7
- 14/08 · BAR · Chrono Trigger as target, Stardew as control, Comix Zone as overshoot · the human's three references do not converge, and that makes them a probe rather than a bar
- 14/08 · SUBJECT · arthropod · a body with a grammar, excellent game territory, and where the model's ceiling is highest — failing there is failing anywhere
- 14/08 · UNIT · the grammar, never the sprite · pixel retouching is drawing; adjusting the grammar is constraint satisfaction
- 14/08 · ANIMATION · transforming anchored parts, not redrawing frames · it is the cut that separates reachable from unreachable
- 14/08 · GATE · animated loops, never stills · the model's still sprite passes for good far more often than its walking sprite — a gate on stills lies in the model's favour
- 14/08 · GATE · find the impostor, not "is this good?" · discrimination is far cheaper than generation, and extracts judgment without demanding direction — the human declared visual direction as his gap
- 14/08 · GATE · the human assembles the sheet · the model does not reproduce published art, and it is the human assembling it that preserves his blindness
- 14/08 · KNOB · height, tones and frames left open · opening the numbers converts them from constants into idiom, and idiom is the probe's axis — the gate sheet takes over enforcing comparability
- 14/08 · KNOB · an open knob declares point and anchor every round · open is negotiable between rounds, never undefined within one
- 14/08 · LOOP · reading of the project: ARTIFACT · a reusable grammar that happens to test the loop; under this reading 3 strikes kill the grammar's ambition, not the project
- 14/08 · LOOP · reusable is harvested, not designed · mark portable/stack on every rule; abstracting over body plans before a sample passes the bar is building the judge before the artifact
- 14/08 · LOOP · high availability raises batch frequency, not batch size · attributability bounds the size and does not move with the calendar
- 14/08 · LOOP · focus week 14–21/08 is a de facto stopping rule at project scope · an external deadline is the cheapest substitute for the known gap in §12
- 14/08 · NAME · claude-ink-2d · chosen by the human; "ink" points at the overshoot and opens the binary question in BACKLOG
- 14/08 · LOOP · cycle open ran clean · INTAKE guard passes, no contradiction in state, instrument debt opened and paid in the same turn
- 14/08 · HARNESS · round zero closed · deterministic core plus perception channel, HARNESS §4 acceptance ticked item by item
- 14/08 · STACK · no runtime dependency; TypeScript executed by Node's own type stripping · vitest, typescript and @types/node are the only devDependencies, and the indexed PNG writer stays unproposed until a sample needs it
- 14/08 · CHANNEL · luminance contact sheet plus 25% silhouette plus per-part pixel ownership · looking catches what is wrong, only counting catches what is absent
- 14/08 · INSTRUMENT · the channel's four null cases run in the suite, not in prose · the rule that was skipped three times is now a test that fails
- 14/08 · LOCK · animation family locked with margin 0.02 of the canvas, not byte-inequality · a twitch of 0.0009 passes equality and fails the margin, measured in both directions
- 14/08 · LOCK · every tunable must carry a named anchor, asserted by test · a free-floating constant breaks on the first knob turn
- 14/08 · CHANNEL · the human's channel is a self-contained HTML page, not an animated GIF · the human overrode the model's recommendation; "never judge a still" is a property of the viewer, not of the file format, and in HTML it is enforced instead of inherited
- 14/08 · CHANNEL · two viewers from one emitter: bench with controls for the model, gate with none for the human · the rule against stills protects his reading, so the model keeps its scrubbing and he does not get any
- 14/08 · GATE · controls and the naming machinery are compiled out of the gate page, not switched off in it · a disabled control is one typo away from an enabled one, and the thing it enables is the end of the reading
- 14/08 · STACK · no game engine; "the sprite in situ" becomes its own axis after the gate · an engine smooths and reintroduces a clock at the point of comparison, and the error has a direction — it flatters the model's own cell
- 14/08 · KNOB · playback at 125 ms per frame and integer scale x4 · 8 fps is the Stardew control's walk; a fractional scale is judged through mud and 1:1 through a microscope
- 14/08 · INSTRUMENT · the viewer's runtime runs headless against a fake DOM, and the visible null case ships beside it · five defects planted, five caught, all five flattering
- 14/08 · CHANNEL · the bench is served and stays open; frames swap under a loop that never stops · a page rebuilt per run is judged from a standstill, and the reload is where the comparison dies
- 14/08 · CHANNEL · the live page recomputes in a fresh process, never in-process · a module cache would serve stale frames while the page claims they are new
- 14/08 · GATE · the gate is only ever a frozen file, never served · a sheet that can change under him mid-reading is not a reading
- 14/08 · ARTIFACT · engine compatibility is an output contract, not an architecture · depending on one renderer would make the grammar unusable to every other engine, which is the opposite of the artifact reading
- 14/08 · LOCK · the export contract is declared and locked before the exporter exists · fixed frame rect, pivot on the grid, named phases, anchors, indexed palette, whole-ms timing — the grammar cannot drift out of exportable while nobody is looking
- 14/08 · LOOP · Pixi enters later as the consumer that proves the claim, never as a dependency · an example that loads the atlas turns "this serves game devs" from rhetoric into a run
- 14/08 · LOOP · every ask states what to open, what to do, how long, what a pass looks like, and what each answer changes, in plain language · the human asked for it; an ask he has to decode gets rubber-stamped, and a rubber stamp arrives looking like data
- 14/08 · INSTRUMENT · the viewer's null case confirmed by the human, all four cases · his eye is the last link the headless locks cannot cover, and it now agrees with them
- 14/08 · GATE · find-the-impostor is unsound as specified and is suspended, not merely revised · whoever collects the five published loops has seen them, so the sixth is theirs by elimination — the human found it, and the intake's own rationale had it exactly backwards
- 14/08 · LOOP · the human judges rounds directly, at his request · accepted with its cost declared: someone who watches dozens of runs learns the model's hand, which is a second and permanent reason no blind-discrimination reading can survive here
- 14/08 · GATE · reading v2 approved: rank mine among five fixed shipped loops, strike is a reading that does not improve on the previous one, three in a row ends it · it needs no blindness, so it survives both the hole in v1 and the human learning the model's hand
- 14/08 · GATE · where the plateau sits decides death from completion: 5th or 6th kills the thesis, 1st to 4th means the ceiling was reached · landing last early means not there yet, never cannot get there, so early readings became free and the trigger could be made aggressive
- 14/08 · GATE · first reading at locks-green on the winning idiom or 21/08, whichever comes first · a trigger that waits for the model to feel ready never fires
- 14/08 · ARTIFACT · reference files never enter the repo; refs/ is gitignored · reference is for looking at, and the thing published here is the grammar
- 14/08 · GRAMMAR · a dark line between any two touching parts, generated by the render · third occurrence of one defect family — abdomen against thorax, thorax against head, legs against the body's shaded side — and shading cannot separate them because it is continuous across the seam
- 14/08 · GRAMMAR · the abdomen is modelled as overlapping plates instead of one ellipse with drawn seams · modelling the real segmentation makes the ridge fall out of the render rule, and the hand-drawn seams doubled with it
- 14/08 · LOCK · every named phase must land exactly on a frame · 5 frames against 4 phases shows one key pose, interpolates past three, and the export manifest rounds and claims otherwise
- 14/08 · KNOB · shading curve, linear only where the baseline lives, 1.8 in the probe · a linear ramp spends half the sprite on the shadow side, and the shadow side is where the silhouette dies
- 14/08 · KNOB · all four probe samples share a 600 ms cycle · a sheet whose cells finish at different times is also comparing walking speed, and 600 divides into whole milliseconds for 4, 8 and 12 frames
- 14/08 · CHANNEL · a cell may declare a rate and a scale off the page's single clock · comparing a 4-frame with a 12-frame walk needs both to finish together, and two resolutions must arrive on screen the same size; what stays forbidden is a cell owning a timer
- 14/08 · ARTIFACT · every generation with a new hash is kept in gallery/ forever, and the suite may not write to it · the human asked for the history; a run regenerated and lost turns "it got better" into a claim instead of a comparison
- 15/08 · CHANNEL · the served page is the only surface: live run first, the whole kept history behind it, self-test at /selftest · a page you regenerate into a file is a page you look at less, and the history is worth nothing if seeing it takes a command
- 15/08 · CHANNEL · the in-memory history ring is dead, replaced by the gallery on disk · two histories with different lifetimes is one history and one rumour
- 15/08 · INSTRUMENT · the gallery directory is overridable, and the suite runs against a throwaway one · protecting the real history with a --no-keep flag guarded it by convention and switched off the very thing worth testing
- 15/08 · CHANNEL · black ground everywhere except the self-test, which keeps its grey · the human asked for black; index 0 is transparent so the ground is the sprite's background, and a black ground swallows the darkest ink — the page that proves defects show cannot be the page that hides one
- 15/08 · CHANNEL · the live page is slides, one generation at a time, arrows to walk the history · a history that only grows is unreadable as a wall; the gate and the self-test stay grids because both exist to be compared side by side
- 15/08 · ARTIFACT · each kept generation carries the resolved tunables and a summary derived by diffing them · a narrated note says what the model meant to do and a diff says what changed, and when they disagree the diff is right
- 15/08 · IDIOM · the human's verdict on the probe: the high-budget sample impressed him most, and the arthropod walk reads · disagreement outranks agreement — the model shipped C expecting to knock it down by looking, and the human looked and picked it
- 15/08 · TASTE · TASTE.md section 1 gains its first derived line, and section 2a its first entry at all · both from one sample, and the entry is not a pull toward polish but a pull toward predicting that restraint wins
- 15/08 · BAR · tone budget per material is no longer empty: 8, from a sample, above the declared 3-6 range · the knob was opened to be negotiated and the negotiation happened on an artifact, which is the only place section 4 allows it
- 15/08 · GRAMMAR · an accessory gets its own material, never the body's · the human read the sash as a buggy arm; it shared the torso's ramp and a limb's taper, so the fix is the family rather than the colour
- 15/08 · ARTIFACT · the history publishes as one self-contained file, no dependency and no server · deployable by dropping a folder, and every lock on the page survives because nothing generates it but the emitter
- 15/08 · CHANNEL · the ground goes back to mid grey, at his call after looking · index 0 is transparent so the ground is the sprite's background, and against black the darkest ink and the outline vanish together
- 15/08 · CHANNEL · a slide is a run, not a sprite: cells sharing a topic land on one slide · a run seen whole is a comparison, a run seen one sprite at a time is a memory test
- 15/08 · CHANNEL · the shell is built in a fresh process and the page reloads only when the viewer itself changed · editing the viewer needed a restart, and a reload for a frame change would restart the animation the live bench exists to keep running
- 15/08 · GRAMMAR · a material that appears in two unrelated places is a material doing two jobs · the ninja's bare forearm read as a stray body part behind the sash; skin now appears only on the face
- 15/08 · ARTIFACT · the gallery is append-only and is never regenerated from current code · regenerating it silently replaced the ninja's "before" with its "after", which is the exact comparison the history exists to hold — recovered from git the same turn
- 15/08 · IDIOM · the round does not close on one winner: all four variations stay live, at his call · they are one grammar sampled four ways, so every change is seen through four idioms at once — but the gate ranks one cell, and that choice cannot wait past the first reading
- 15/08 · LOOP · the bar is challenged every iteration, not once at the end · his expectation, and the progression lane is what makes it visible: each generation is an attempt at the previous ceiling
- 15/08 · GATE · interim reading until the five references exist: did this beat the previous one, read off the progression lane · declared weakness, and it is the project's own known trap — an internal bar can rise forever without ever approaching the commercial one, so it expires the moment the references land
- 15/08 · CHANNEL · two lanes, his design: progression is the arc, requests is what he asked for · keeping them apart stops his asks from being read as my progress
- 15/08 · AXIS · silhouette and value: two variants, line against value, both rendered and kept · the idiom he praised is the one whose edge dissolves, so the round attacks its weakest point rather than its strongest
- 15/08 · GRAMMAR · the value edge is a render rule and a knob: edge pixels go to the ends of their own material's ramp, lightest into the light · a rim and an occlusion edge are how art with no outline still reads as a shape
- 15/08 · LOCK · silhouette at 25% must stay one body, and the edge must clear 0.10 luminance from the ground · calibrated on the failing sample at 0.058, and null-cased with the subject switched off so an empty strip cannot score perfectly
- 15/08 · ASSUMPTION · the ink question stands unanswered; proceeding as (A), aspiration, as declared in advance · recorded as an assumption and not as his answer
- 15/08 · OPEN · the ninja's neck reads as bare skin and is deliberately not fixed, at his call · root already diagnosed: the mask covers the crown and leaves the chin, when a ninja is the other way round
- 15/08 · CHANNEL · one slide per run, and the page shows runs only · his structure, stated flatly after the model built two other things instead; there is no live cell, because work in progress is the model's business and the page is where a verdict is given
- 15/08 · CHANNEL · keeping is a deliberate act at the end of a batch, never a side effect of an edit · auto-keeping shattered the history into micro-generations nobody had been asked to judge
- 15/08 · CHANNEL · the history keeps every version, the page shows the latest per element · that is how "the humanoid must be the most recent version" is satisfied without deleting the version before it
- 15/08 · LOOP · the silhouette round does not count as a run · it fixed a measurable defect and raised nothing; the human's test is "did it beat the ceiling", and a correctness fix answers a different question
- 15/08 · LOOP · the model proposes the theme of each run and the human approves or replaces it · his reasoning, and it is better than the model's: a theme he picks may be one the model cannot draw, and then the reading measures the brief instead of the ceiling
- 15/08 · CHANNEL · the dummy leaves the page, at his call · it is the harness fixture and was never content; putting it on the judging page was the model's choice and it duplicated the arc
- 15/08 · RUN 3 · theme: three-quarter view, three creatures, one grammar · a side view resolves a walk and never passes for published art, because nothing the bar games ship is drawn from the side
- 15/08 · GRAMMAR · from three-quarter the far limb row keeps half its length, not seven tenths · at 0.7 the far legs read as a second creature standing behind the first; what reaches the eye from that side is tips
- 15/08 · GRAMMAR · a rim light is narrow, not half the silhouette · the first rule lit every edge leaning toward the light, which flattened the volume it existed to give
- 15/08 · LOOP · the model breaks CLAUDE.md section 5 knowingly this round: three body plans is generalising before one sample has passed the bar · the human asked for the stretch, and the break is recorded rather than quiet
- 15/08 · GATE · first reading taken: run 3 did not beat run 2 · strike 1 of 3, and the counter moved in BACKLOG the same turn
- 15/08 · GRAMMAR · depth is a step along a material's own ramp, never a different material · the human saw the far limbs as a lighting error before the model did, and he was right: the same lamp lights both sides
- 15/08 · GRAMMAR · far-side limbs keep 0.85 of their length, after 0.7 and 0.5 were both wrong by eye · foreshortening at this angle is gentle, and a limb half its twin's length is a limb from another animal
- 15/08 · GRAMMAR · each leg station carries its own rest angle instead of a linear fan · a beetle's hind pair points at its rear; an even fan gives a beetle three pairs of middle legs
- 15/08 · TASTE · TASTE.md section 2b opens with two entries, both from run 3 · articulation density is the ceiling rather than perspective, and the model reaches for a change of vocabulary where a step within one is correct
- 15/08 · LOOP · the mantis and the scorpion are retired rather than repaired · the model does not believe one pass reaches run 2's level with them, and saying so is worth more than a second attempt that hides the same finding
- 15/08 · GAIT · the tripod wins over the metachronal wave, on his eye · "o tripé ficou natural"; the wave was implemented because he read one off video, and looking settled it in one minute
- 15/08 · GRAMMAR · the antennae point forward and up · they pointed at the floor because a capsule grows down-screen at angle zero and everything else on this body hangs downward — the whole file inherited the habit
- 15/08 · LOOP · a geometry change the human had already approved was reverted the same turn it was made · hanging the far leg row downward was the model's own reading, applied to a row he had just praised; §2a already records the model predicting verdicts before they are given
- 15/08 · GATE · run 4 did not beat run 2 either · strike 2 of 3, and his words matter more than the verdict: a few more iterations would reach the same level, which is parity, and parity scores a strike
- 15/08 · LOOP · iterating the beetle is the wrong bet for the last reading · by his own account it converges on run 2 rather than past it, so spending the third reading there ends the thesis by arithmetic
- 15/08 · OPEN · the antenna does not read as an antenna — "se essa estrutura conectada na cabeça é uma antena" · a straight 6 px stub at 0.9 radius is a spike; length, thinness and a bend are what name it, and that is a shape problem rather than an angle one
- 15/08 · GATE · the end condition moves from three strikes to the date, 21/08 · his call, and it is right: a counter made the model reason about spending its last life, which pushes toward the conservative run exactly when the experiment needs the ambitious one
- 15/08 · GATE · per-run readings continue and inform direction; they no longer kill · the signal about direction is worth keeping, the distortion is not
- 15/08 · SUBJECT · a gorilla, proposed by the human · it lands on the model's own recorded strength — one heavy mass carrying the reading — and on its recorded weakness, since fur has no segmentation to hold a silhouette
- 15/08 · SUBJECT · the gorilla reads and the weight reads, on his eye · "dá pra sentir seu peso caindo e a fidelidade com movimento" — the first time an axis other than form has landed
- 15/08 · RUN 6 · a transformation, his proposal: the gorilla becomes a robot · it forces the mechanical vocabulary the model had just admitted it lacks, and a robot of ellipses would read as a soft toy
- 15/08 · GRAMMAR · a transformation is a ping-pong cycle, not a one-way sequence · played once it snaps at the loop point; played both ways it is a genuine cycle and the mechanism is seen twice, with no engine change at all
- 15/08 · GRAMMAR · mechanical is `rect` plus the inner-outline rule · the primitive has existed since round zero and had been used once; what was missing was the nerve to use it, not a new shape
- 15/08 · GRAMMAR · a part arrives and leaves by **scale** on a bone of its own · plates hang off their own bones so metal can vanish without taking the limb underneath with it
- 15/08 · KNOB · the transformation does not share the project's 600 ms cycle · the shared duration existed so walks could be compared, and this is not a walk
- 15/08 · LOOP · cycle open run cold: 53 locks green, tsc clean, and four contradictions found inside BACKLOG · a gate block saying "no reading taken yet" above a table of two readings, a retired counter still threatening death in prose, a "next round" naming an axis three runs stale, and a held-for-human item asking approval for a reading already adopted — all fixed the same turn
- 15/08 · CORE · depth becomes a solver, not an authoring decision · his proposal, and it is 3D used to remove constraints from a 2D world rather than to leave it: a z-buffer, 2.5D, no projection divide, so depth decides who is in front and never where anything is on screen
- 15/08 · GRAMMAR · three defect families had one root cause, and the third occurrence is where the method says generalise · far limb reading as a lighting error, two parts of one material fusing into a mass, and foreshortening guessed by eye were all paint order standing in for depth
- 15/08 · GRAMMAR · the primitives were already solids and depth cost a field, not a rewrite · ellipse to ellipsoid, capsule was always a swept sphere, rect to rounded box — the vocabulary chosen on day zero happened to have a third dimension for free
- 15/08 · GRAMMAR · `shift` keeps its numbers and changes its job: it is the cheat knob now, not the depth system · supersedes nothing in the 15/08 ramp line — a limb still moves along its own ramp; what changed is that occlusion is computed and the extra step down is the deliberate lie art takes past what the light would do
- 15/08 · GRAMMAR · the inner-outline rule's intent was always "darken the one behind" and its predicate finally became true · it asked paint order because paint order was the only thing that knew; a limb swinging in front of a mass it was declared behind got the seam on the wrong side, and nothing shipped had a pose extreme enough to show it
- 15/08 · GRAMMAR · a part carries its own depth as well as its bone · detail sinks inside mass otherwise: brow, fist, foot and shoulder all vanished on the first authoring pass under the solver, and it is one defect, not four
- 15/08 · HARNESS · a tunables leaf the core reads is checked at load · the files are JSON behind a cast, so adding light.z to the renderer and missing it in one of twelve paints a blank sprite behind a green suite — the exact failure shape HARNESS §3 records as having shipped before
- 15/08 · RUN 7 · the gorilla jumps and hits, and the robot is dropped · his call: validate the body by giving it things to do rather than another costume. The jump is the weight test on the one axis other than form that has ever landed here; the attack is the depth test, because a fist behind the torso and then in front of it is one part on both sides of a mass
- 15/08 · GRAMMAR · two limbs on the same arc are one limb, and the solver is right to draw one · the far foot came back absent in all twelve frames of both actions; the fix is a staggered stance, never a nearer z, because a far limb pulled forward to be seen punches through the body
- 15/08 · KNOB · an action needs more amplitude than a walk, and the absence lock is what proved it · below ~36 degrees no limb clears the torso outline, so only being in front was keeping the near arm visible; jump at 0.22 turn and attack at 0.40 against the walk's 0.10, each the smallest value that keeps every part present
- 15/08 · LOOP · the engine changed mid-week and the walk was re-rendered unchanged beside it · same gait, same body, one variable — without that pair a bad reading could not be attributed to the engine or to the two new actions
- 15/08 · RUN 7 · his verdict: "não sinto uma mudança visual, mas a movimentação e os detalhes estão melhores e mais fluídos" · the control did its job — the walk is run 5's gait through the new engine and he reports no damage to it, so the solver stays and nothing is rolled back
- 15/08 · TASTE · the model predicted the shading rewrite would be visible, and it was not · real 3D normals replaced a distance-to-edge sweep on every pixel of every part, and the human's eye reported no change of look at all; what he did report was detail and fluidity, which is the depth half of the change and not the lighting half
- 15/08 · LOOP · a technically large change can be perceptually invisible, and the round only knows which because a control was rendered · without run 5's gait re-rendered untouched beside the two new actions, "gostei" would have been unattributable across an engine and two gaits at once
- 15/08 · TASTE · his diagnosis: form and movement are ahead, the surface is behind · "encontramos uma forma primorosa de desenhar a forma e movimento, mas a tinta dessas formas ainda precisamos aprimorar" — and he named the bias correctly before I did: seven runs shipped one ink idiom on a single verdict from a single insect probe
- 15/08 · LOOP · the model predicted the quantisation failure two turns before it was reported, called the mechanism to fight it, and then shipped without fighting it · a correct prediction filed instead of turned into work is worth the same as no prediction
- 15/08 · RUN 8 · frontier probe on the ink axis, his design: one jump, three idioms · body, skeleton, parts, gait, canvas, scale, frame count and every gait amplitude frozen identical, so whatever the three disagree about is the ink
- 15/08 · GRAMMAR · an ink idiom is a palette, not a set of knobs · the tone budget is a lock (ramp length equals tones.perMaterial), so "spend four instead of eight" cannot be a dial — which is the lock working, and it makes each idiom a grammar over the same body
- 15/08 · HARNESS · the outer outline was 4-connected and left the diagonal corner of every staircase open · latent since round zero and invisible because both shipped idioms turn the line off and carry the edge with rim; now 8-connected, and locked as "if there is a line, the line owns the whole silhouette"
- 15/08 · GRAMMAR · the jump ran off the bottom of the cell at the absorb pose, and had done so in run 7 · the arc is 53 px of a 64 px cell and was merely 6 px too low; the value lock only caught it once an idiom with an outline existed, because rim had been pushing the clipped pixels to a ramp end
- 15/08 · MEASURE · tone-region size is the ink axis's missing instrument, and it confirms his reading with a number · the same 667 body pixels are cut into 195 regions by the incumbent and 95 by the control, and 46% of the incumbent's regions are a single pixel — noise wearing the name of shading. Not compiled into a lock: the axis is still a probe, and building the judge before the artifact is TASTE-LOOP §6
- 15/08 · OPEN · two pre-existing defects surfaced by the repo-wide sweep and deliberately not fixed this round · probe-d clips 12 border pixels and scorpion sits at 0.079 edge contrast against a 0.10 floor; both are retired samples, and repairing history is not what the log is for
- 15/08 · GATE · run 8 ink probe, his ranking: **Chrono first; Stardew and the incumbent tied last** · the seven-run incumbent idiom loses on a complex body, and the ink axis is settled enough to author against
- 15/08 · TASTE · the tie is the finding, not the winner · wide value range without regions loses, regions without range loses, and only both together win — a two-factor result the probe's bracket produced and a Chrono-only sample would have hidden behind "fewer tones is better"
- 15/08 · TASTE · prediction scored: half right · "the incumbent loses and Chrono wins" landed; "Stardew also beats the incumbent" did not, and the miss is exactly the factor I had collapsed
- 15/08 · SUBJECT · a tree, his reference, with wind in the leaves and leaves falling · first non-animal subject, and the first where the gait is not locomotion at all
- 15/08 · GRAMMAR · a crown is three masses with foliage on them, not seven self-contained clumps · two passes came back as a sponge before the count was measured: 28 foliage parts inside a 30x28 px crown, which is the exact limitation TASTE.md 2b has recorded since run 3 and which I authored straight past, twice
- 15/08 · HARNESS · a part at scale 0 painted exactly one pixel, and had since run 6 · at s=0 the inverse scale is forced to 0, every candidate pixel maps to the shape's centre, and a centre is always inside its shape — invisible among the mech's 36 plates, surfaced by a falling leaf that would not reach zero on its way out
- 15/08 · GRAMMAR · wind is lag travelling down a hierarchy, and it is the walk's head-lags-chest rule applied recursively · trunk peaks at gust, branches at peak, clusters at ease, so the tree is in three states at once and a wave reads instead of a wave hello
- 15/08 · GRAMMAR · a detached part is a bone with a trajectory plus a scale window · falling leaves needed no new concept — the machinery that made run 6's plates arrive and leave does it
- 15/08 · KNOB · one gait, two motions three times apart in size, one amplitude · a branch sways 10 degrees and a falling leaf tumbles 90, both on the angle channel: the amplitude belongs to the largest motion and everything else is authored as a fraction of it. Run 7's lesson one level in
- 15/08 · TASTE · his verdict on the first tree: "ficou horrível" · and it is the correct verdict — three defects, one of them structural and two of them mine
- 15/08 · GRAMMAR · **the vocabulary had a hole, not a tuning problem: every primitive was convex, and the union of convex solids is smooth** · a subject whose form lives in a ragged boundary — foliage, cloud, rock, flame, torn cloth — was inexpressible, and no amount of moving ellipses could have found that out
- 15/08 · CORE · first new primitive since round zero: `lobed`, r = R(1 + depth*cos(lobes*theta + phase)) · closed form in theta, no noise and no seed, and it null-cases against the ellipse it generalises: at depth 0 the two are pixel-identical
- 15/08 · GRAMMAR · the crown composition was upside down and that was the biggest single defect · the reference makes foliage the silhouette and shadow the interior; I made shadow the silhouette and foliage the interior, which is a mushroom cap with spots. 0% of the boundary was leaf, now 60%
- 15/08 · GRAMMAR · a subject read by its EDGE defeats an engine that describes VOLUME · normals, depth and ramps all model volume, which is why a gorilla — two masses and four limbs, literally ellipsoids and capsules — had no translation loss and a canopy had nothing but
- 15/08 · KNOB · light.curve goes below 1 for the first time, at 0.75 · above 1 the ramp is spent on the lit half; a dark body with a narrow bright edge needs the opposite, and at 1.1 a 14 px trunk read as the lightest thing in a picture whose reference trunk is the darkest
- 15/08 · LOOP · two fixes were applied to one defect and one had to come back off · curve 0.75 plus shift -1 on the trunk killed the highlight the reference is built on; the shift was reverted after measuring rather than after looking
- 15/08 · SUBJECT · his direction on the tree, held for the next pass: more leaves, clump radius about 2x, and a more discreet sway · recorded rather than applied, at his request — and recorded at all because "prose judgments evaporated overnight" is the named failure this log exists against
- 15/08 · INTAKE · the project's purpose, stated plainly by him on 15/08 and it reframes everything above · he cannot draw, many people cannot, and he CAN say what he wants in a game. The question under test is whether drawing can be economically delegated to the model — and drawing is one discrete piece of a harness plus engine meant to let people build games without being stopped by what they cannot do
- 15/08 · HARNESS · reading in text is not a poverty measure, it is the economy thesis under test · his words: it is what he found in other repos with me and it IMPROVED the drawing. The hypothesis for why: a channel that cannot show pixels forces every fix into the generative representation, which is CLAUDE.md section 5's no-retouching rule enforced by blindness instead of by discipline
- 15/08 · LOOP · his "naive" questions are harness probes and should be answered as such · asking whether leaves follow a pattern is this project's own founding premise — drawing as constraint satisfaction — applied one level deeper than the model had applied it
- 15/08 · GRAMMAR · **the tree's skeleton is 20 hand-written bones, which is one tree expressed as data and not a grammar for trees** · turning bodies into constraint satisfaction was done; turning branching structure into it was not, and an L-system is the named next step
- 15/08 · CORE · the lobed primitive sums octaves: amplitude halving, frequency doubling, offset by the golden angle · fractional Brownian motion evaluated on a circle, closed form because the noise is a cosine. `depth` stays the total swing whatever the octave count, so ragged-ness and scale-count are orthogonal knobs
- 15/08 · SUBJECT · his direction applied: eleven clumps up from seven, bigger, and sway down from 0.2 to 0.09 · the outermost clump now travels 0.5 px across a 44 px canopy, and 65% of the silhouette is foliage
- 15/08 · GRAMMAR · two parts deleted for rendering zero pixels rather than nudged into view · a denser canopy buries its own centre, and the absence count's job is to find parts not earning their keep, not only parts that broke
- 15/08 · PROJECT · **scope redefined by him, and CLAUDE.md rewritten whole** · the goal is to turn the model into a draughtsman of game objects; the project fails if that is not possible. Superseded is not wrong: what the old file said was true of a narrower project
- 15/08 · PROJECT · breadth is now the deliverable, not one excellent subject · a draughtsman handles many kinds of object and many kinds of animation, so "do not generalize before it works once" is retired — it worked, and generality is the work now, under the surviving constraint that it is harvested and never designed
- 15/08 · PROJECT · **compatible outward is a requirement, not a wish** · an engine that only runs in this repo is not an artifact; the exporter moves from deferred to third in the build order, and "exporting probe art is inventory" dies with the reasoning that justified it
- 15/08 · STACK · web/Pixi is the first proof target, his call · same language as the core, so it is the cheapest route to something that actually runs; Pixi is a consumer in an example and never an import of the core
- 15/08 · GATE · **version 3, the commission test, adopted** · he names an object and an animation in one sentence, the model delivers with no back-and-forth, he answers in one word. Two numbers: hit rate, and cost to hit
- 15/08 · GATE · "ships" means usable AND he would be glad to have it — his call, the higher of two bars offered · declared cost: one verdict carries two questions, so a miss does not say which half missed. Mitigated by one word of reason on a miss
- 15/08 · GATE · **gate v2 retired unrun, exactly as v1 was, and the pattern outranks both** · v2 needed five files he had to collect and they never arrived across a week — and the model asked three times without once saying plainly what they were. Two gates specified, two never fired, both with setup cost landing on the human. v3 costs one sentence and one word
- 15/08 · GATE · the 21/08 date is retired with the conditions it belonged to, at his call · the stopping rule is now the gate's two numbers, which can say "no" and not only "time is up" — and the known gap it reopens is named rather than papered over
- 15/08 · HARNESS · **the perception channel is a first-class axis, measured by cost to hit** · his stated central risk, and his reframing: the no-images rule is about the most effective way for the model to draw, because validating is part of drawing
- 15/08 · HARNESS · the channel gets enriched toward answering questions about parts, never toward showing a better picture · a channel that cannot show pixels forces every fix into the generative representation, which is the no-retouching rule enforced by blindness instead of discipline
- 15/08 · GATE · **his correction, one turn after v3 was written: no numeric kill** · "esse projeto falha quando eu decidir que falha... não vou ter uma métrica simples pra aplicarmos neste repo". The stopping decision is his, held in his head, and it is allowed to be
- 15/08 · GATE · **a miss is a specification, not a strike** · five misses are a prompt to design the missing capability, not a verdict to quit — which is what every miss in this project has actually done, from the far limb that produced the z-buffer to "ficou horrível" that produced the lobed primitive
- 15/08 · TASTE · third gate design, third correction, and the pattern is one direction · v1 hid the answer from him, v2 dumped the setup cost on him, v3 tried to automate his verdict. The pull is not toward rigour, it is toward not having to ask — recorded in TASTE.md §2a as portable
- 15/08 · GATE · the known gap "no stopping rule" is answered behaviourally rather than mechanically · the party with the incentive to continue is the model, so the safeguard is unsoftened reporting of misses and predictions scored before he looks — his judgment arriving well-fed rather than pre-empted
- 15/08 · LOOP · language rule: reports to him use ASD-STE100 Simplified Technical English, or simplified aeronautical Portuguese · his request, and it makes exact a requirement CLAUDE.md section 4 already stated approximately. Scope is reports, not repository files
- 15/08 · HARNESS · **the findings channel ships** (`src/perception/structure.ts`) · seven checks, each one a throwaway script from this session kept: absent, mostly-hidden, thin, flat, clipped, silhouette ownership, tone-region structure. It answers questions about parts and never draws a picture
- 15/08 · HARNESS · the channel found a regression in a judged sample on its first run · the beetle's two elytra render 0 px in all frames, confirmed independently by metrics.partPixels. They carry no `z`, so the run 7 depth solver let the body mass win over them. **80 locks did not catch it**
- 15/08 · HARNESS · **a check was withdrawn before shipping because it could not be calibrated in both directions** · value-collision fired on almost every part of every healthy sample when measured by mean tone, and on none when measured by fraction near the ground. Replaced by `flat`, which fires and stays quiet on demand
- 15/08 · HARNESS · the instrument's own failure mode is the inverse of flattery, and it has its own lock · a check that alerts on healthy work trains the reader to skip the output. The guard asserts silence on the idiom he ranked first and noise on the idiom he ranked last
- 15/08 · OPEN · the beetle regression is recorded and not repaired · the subject is retired, and repairing history is not what the log is for. The finding stands as evidence that the depth solver had a cost nobody had counted
- 15/08 · CORE · **self-shadow ships: the depth buffer's second use, and the first that changes the picture** · run 7 built the buffer, spent it on paint order and discarded it, which is exactly why that round bought correctness and no visible change
- 15/08 · GRAMMAR · the shadow edge is hard and its strength is in ramp steps, not opacity · follows the ink verdict of 15/08 — regions with a boundary beat gradient, and a soft falloff would spend the middle of a ramp that is already spent
- 15/08 · SCOPE · **no ground shadow inside the sprite** · a sprite has no ground and the game has one; baking a floor into a 64 px cell decides something that belongs to whoever places the sprite. It goes in the Pixi scene, phase C
- 15/08 · KNOB · shadow steps is 8, measured rather than chosen · a sweep at 6, 10, 16 and 24 returns identical shadow area from 6 onward — past a few pixels the ray has left the body
- 15/08 · KNOB · shadow bias is 0.4, and the findings channel priced the trade · 0.75 covers 5.3% of the body and 0.4 covers 8.7%; the cost of the lower value is surface acne, measured as tone-region fragmentation at 230 to 239 on the tree. **The instrument built one hour earlier is what made this a measurement instead of a preference**
- 15/08 · OPEN · the light direction is now measured as costing shadow, and it is the second time it has surfaced · the lamp is upper-left and the gorilla faces right, so the near arm throws its shadow into empty space: 5.3% coverage against 7.9% with the lamp mirrored. Not changed unilaterally — it is a knob he has never judged
- 15/08 · GATE · his verdict on the shadow: "vejo o sombreamento em ambos" · and it scores a prediction of mine at WRONG — I predicted he would see it on the tree and not on the gorilla
- 15/08 · HARNESS · **the shadow was making acne, and a bias cannot fix acne** · a lone convex sphere shadowed itself: 198 px at radius 18, and still self-shadowing at eight times the tolerance, because acne scales with curvature and a constant tolerance does not
- 15/08 · CORE · a part never shadows itself, enforced by identity and not by tolerance · every primitive here is convex in depth, so a ray landing back on its own part has found acne rather than an occluder. Acne now measures zero at every radius and every bias
- 15/08 · HARNESS · **the acne was inside what he approved, and the correction is worth more than the approval** · the tree was 98% real shadow, the gorilla only 62% — about 38% of what he saw on the gorilla was the defect. Told rather than quietly fixed
- 15/08 · HARNESS · my first acne measurement was insensitive and I acted on it · region fragmentation on real subjects showed 230 against 239 and I called the trade cheap; real subjects have many parts, so legitimate shadow masks acne. The right null case is a single convex part alone in the frame, which is unambiguous
- 15/08 · KNOB · shadow bias drops to 0.25 and its job changed · it no longer guards a surface against itself; it only arbitrates a tie between two different parts meeting at nearly the same depth
- 15/08 · CORE · **the fill light ships at weight 0.15, and the weight is the measured edge** · his ink verdict requires both a wide value range and clustered regions, and this knob moves them in opposite directions. Swept at seven values: regions improve monotonically, and the tree loses 23% of its range between 0.15 and 0.17. 0.15 is the largest weight that costs no range on either subject and still buys 7% in region size
- 15/08 · HARNESS · the singleton fraction is not monotonic in noise and no lock depends on it · at heavy speckle the dropped pixels touch and form blobs, so the proportion of one-pixel regions falls while the region count doubles. It is printed and never asserted
- 15/08 · LOOP · two locks failed because the test guessed geometry the engine had right · a lamp below a caster that sits above the receiver casts into empty space, and the shadow band starts beside the caster rather than under it. Both assertions were rewritten from the geometry instead of from expectation
- 15/08 · CORE · **the capsule tapers: `r1` runs the swept radius linearly from one end to the other** · a limb, a horn, a tail, a branch and a blade all narrow, and the only way to say so was stacking capsules of decreasing radius — three parts for one shape, each seam left for the render to reconcile
- 15/08 · CORE · the taper's closest point slides along the axis by the slope, so it is one quadratic and not a projection · at zero taper every term collapses back to the plain projection, which is why nine runs of authored capsules render byte-identical
- 15/08 · CORE · **non-uniform scale ships: `scaleX` and `scaleY`, in screen axes** · a body flattens downward whatever angle its limbs are at, which is what a landing does and what a per-limb scale could never express. `Xform` carries sx, sy and sz separately
- 15/08 · GRAMMAR · depth carries only the uniform part of scale · flattening a body on screen must not reorder which of its parts is in front, and a landing that reordered the limbs while it landed would be a different defect every frame
- 15/08 · GRAMMAR · shading is computed before the squash and painted onto the squashed shape · the exact answer is an inverse-transpose on the normal; this is the approximation 2D animation has always used, and it is declared in the type rather than left as an omission
- 15/08 · SUBJECT · the jump gets the squash it wanted in run 7 · width over height runs 1.20 at the crouch, 0.70 at the top of the launch and 1.24 on the landing frame, which is the flattest of the cycle
- 15/08 · SUBJECT · every limb on the gorilla and every branch on the tree now tapers · tone regions on the gorilla improved from 107 to 93 and mean size from 7.9 to 8.9 — fewer and larger, which is the half of his ink verdict that was still weakest
- 15/08 · PROJECT · **the product is a game engine whose interface is a coding agent** · his words: "o GameMaker dos agentes", and an engine that lets a user build a game through coding agents. Drawing becomes subsystem one rather than the deliverable
- 15/08 · PROJECT · the reframing renames what exists and discards none of it · every harness property was built so the model could draw unwatched, and each is what any agent needs to build anything unwatched. The same constraint produced the same solution twice
- 15/08 · PROJECT · **the exporter is dead** · it answered "compatible outward", and exporting into somebody else's engine is a bridge to a competitor. The requirement it served is replaced by a harder and testable one: can a FRESH agent, given only this repository, build a small game
- 15/08 · SLICE · a one-screen platformer, his call · gravity, box collision, a jump, a fixed camera — and it reuses the most of what exists, because the gorilla already jumps
- 15/08 · ARCH · the engine's core is a deterministic headless simulation and rendering is a consumer of it · the same rule as sprite() and the viewer, now load-bearing twice: a recorded input sequence must replay to the same state or an agent cannot verify a game at all
- 15/08 · HARNESS · **the differentiator is the agent's perception of a RUNNING GAME, not the platformer** · a text readout of the simulation plus a findings list — the player never reached the goal, the player fell through the floor at frame 143, this gap cannot be cleared by any jump
- 15/08 · GATE · the gate stays on drawing for now, his call · drawing is the only subsystem with a measured result, and an engine gate with nothing to read is exactly how v1 and v2 died. It is born with the first playable slice
- 15/08 · LOOP · drawing item 4, pattern inside a part, is deferred rather than dropped · no commission has failed for want of it, and building it now would be designing generality instead of harvesting it
- 15/08 · PROJECT · **drawing capacity is upstream of the engine, and the engine waits** · his rationale: the greater the production capacity, the greater the diversity of games possible. An engine with a thin asset pipeline makes thin games and no engine work fixes that. No secondary track
- 15/08 · LOOP · **"finish the drawing" has a definition and it is the gate, not a feature list** · commissions shipping reliably across kinds. Gate v3 has never run, so the next step is a batch and not another feature — a miss is a specification, and a feature chosen without one is a guess
- 15/08 · TASTE · second ordering correction in two turns, and they share a shape · when he reframed to "engine" I made the engine the main line and demoted the only subsystem with evidence and a running gate. I sequence toward the newest stated goal instead of asking what it depends on
- 15/08 · SCENE · **the scene compositor ships, and it is the first thing here that is not a sprite** · his request: see the objects in a game rather than as loops on a page. A loop shown alone is judged against itself; a loop standing next to six others is judged against them
- 15/08 · TASTE · **sheet cohesion was declared on 14/08 and measured for the first time on 15/08, and the answer is bad** · 127 colours in the merged palette, 11% reuse, and every reused colour belongs to the mech extending the gorilla. Eight subjects share zero colours with each other. They are a collection, not a set
- 15/08 · GRAMMAR · **a sprite tuned against one background is not portable to another, and it is measured** · four candidate skies against eight subjects: a dark sky loses four, a light sky loses four by the opposite mechanism, and only a mid value clears all eight. Every subject was tuned against the viewer grey and they collectively span the whole value range
- 15/08 · GRAMMAR · a palette and its backdrop are one decision, not two · the consequence of the line above, and it binds any agent placing generated art into a world
- 15/08 · GRAMMAR · **every subject is authored at hero scale and none has a small version** · all eight measure 39 to 58 px tall. At scene proportions the arthropods drop to 15 px and stop being legible, which is a gap no commission has named yet
- 15/08 · SCENE · a subject is placed by its feet and never by its origin · an origin is wherever its author put it — the hips on the gorilla, the base of the trunk on the tree — so aligning origins puts nothing on the floor. The compositor measures the lowest painted row across the cycle
- 15/08 · SCENE · each subject plays a whole number of its own cycles inside the scene's · 600, 780, 1200 and 1760 ms cycles in one 1200 ms scene; two needed retiming, and without it the loop point stutters
- 15/08 · FORMAT · **every future deliverable is a micro game, not a sprite** · his words: an object he asks for is born belonging to a game, so a cloud arrives as a sky a cloud crosses. `bin/micro.ts` is the shelf and he says when a new one starts
- 15/08 · FORMAT · the shelf renders live from current code and the gallery stays frozen · opposite rules on purpose — the gallery is the record and shows what a run was, the shelf is the product and shows what everything is now
- 15/08 · TASTE · **his reading of the first scene beat my prediction, and on the cause** · I predicted the arthropods would clash on palette and size. He answered camera: the tree, the ninja and the gorillas are side view and the insects are three-quarter from above. Mixing camera angles is deeper than mixing palettes
- 15/08 · GRAMMAR · **camera is a property of a set and the grammar has no notion of one** · named by his reading, and it is why the arthropods became a second micro game rather than a corner of the first: showing the defect instead of arranging around it
- 15/08 · SCENE · shrinking a subject to fit a scene does not scale its detail down, it never draws it · his words, "os objetos perderam resolução". Subjects now keep the scale they were authored at and the scene makes room instead
- 15/08 · OPEN · three tracks feed the shelf from here: the drawing system, the engine's components, and the creation methodology · the third is how the agent guides a person building their game, and he places it late — around the end of the following week
- 15/08 · FORMAT · **the shelf is a routed webapp, not a slide deck** · his ask: the home is a shelf of every micro game, clicking one opens it, and each has its own route so iterating is edit-refresh-look. `node bin/micro.ts`, port 5177
- 15/08 · HARNESS · every route renders from current code on every request, with no cache and no build step · a refresh IS the iteration loop, which is the opposite of the gallery freezing an entry so a refactor shows as a difference
- 15/08 · HARNESS · **a server that fails to bind silently serves someone else's pages under our routes** · 5174 was already taken on this machine and every route came back identical from a process that was not ours. The bind error is now fatal and says so, and the port is a flag
- 15/08 · FORMAT · shelf cards decimate to every third frame · a card is a thumbnail, and the shelf otherwise ships three times the bytes to say the same thing. The rate is scaled to match so a card runs at the speed its game runs at
- 15/08 · RUN 12 · **one tree becomes trees: `makeTree` harvests run 9 into a function** · twenty-three hand-placed bones and thirty-eight hand-placed parts become arguments and arithmetic. Nine trees, heights 36 to 66 px, no two alike
- 15/08 · GRAMMAR · every argument to `makeTree` was already a literal in run 9 · harvest and not design (CLAUDE.md section 5). The only additions are the three a FOREST demanded and a single tree could not: a lean, a seed, and a choice of foliage ramp
- 15/08 · GRAMMAR · **one palette for the whole wood, and it is the cohesion lesson applied** · the scene reading measured eight subjects sharing zero colours; a forest of trees each carrying its own greens would repeat that at speed. 25 colours for any number of trees, and a tree picks which of three leaf ramps it wears
- 15/08 · GRAMMAR · clumps are laid out by the golden angle around the crown rather than by hand · no two land in line, and no two trees in the wood show a repeated arrangement — the same trick a plant uses for the same reason
- 15/08 · GRAMMAR · a wood varies its trees by ARGUMENT and never by scale · shrinking a tree to make it look distant re-renders it with less detail, which is the defect he named on the first scene
- 15/08 · LOOP · the outline lock caught all nine trees clipping the cell on the first build · the origin was guessed at canvas.h - 6 and the widest tree's mass reaches 8.5 units below its origin. Derived and fixed in one pass
- 15/08 · GATE · his reading of the forest: "ficou ruim" · three specific causes, all correct — the crowns were all the same, the heights read level, and the clouds jump at the loop
- 15/08 · GRAMMAR · **the crowns were identical because I varied numbers and not topology** · every tree had three branches and its clumps in one ellipse, so no argument could ever have made two crowns a different shape. `form` changes where branches leave the trunk and where foliage sits on them: round, conifer, umbrella, bushy, weeping
- 15/08 · GRAMMAR · the heights read level because the placement cancelled them · the tall trees had been put nearest, which lowered their tops back into line. Height now decides depth — tallest furthest back — and the canopy line varies 84 px against a 12 px band before
- 15/08 · GRAMMAR · girth is capped at 8 rather than linear in height · a 62-unit conifer came out with a trunk 12 units thick, which is an oak wearing a conifer's crown. Real trunks do not scale linearly, and the outline lock found it as a clipped cell
- 15/08 · LOOP · a python replacement swallowed the loop it was meant to edit, and the skeleton's parent-before-child rule caught it immediately · the ordering rule written on day zero as a determinism guard did a second job nobody designed it for
- 15/08 · CLOSED 16/08 · ~~the gorilla is still placed and not controlled~~ · delivered with the live runtime: input, state and draw per animation frame · it needs the browser to run input, state and draw, which is the structural change he explicitly blessed. Not started this session
- 15/08 · GATE · his second reading of the forest: "não gostei nem um pouco" · four causes, all correct — the trees are misshapen, the animation is identical on all of them, some trees are not on the ground, and the forms are not varied. He also named the fix from his reference: variation in the TRUNK, in the BRANCHING, and in the CROWN's circumference
- 15/08 · GRAMMAR · **a tree is recursive, and a flat structure could never have been varied** · trunk to arm to tip was fixed at three levels, so an argument could only change a number and never how the tree is assembled. `grow` calls itself: a branch is a smaller tree. The five hand-written "forms" are gone
- 15/08 · GRAMMAR · the crown has no layout at all any more · foliage attaches to whatever the last generation of branch turned out to be, so the crown's outline is a CONSEQUENCE of the branching. The old crown was placed independently of the arms, which is why bark-coloured masses sat in open air
- 15/08 · GRAMMAR · one mechanism covers both growth habits with no special case · `whorls` is how many tiers of branch leave the trunk. Many tiers on a slow-tapering trunk is a conifer; one or two high tiers on a fast-tapering trunk is a broadleaf. Botany calls these excurrent and decurrent and it is one argument, not two code paths
- 15/08 · GRAMMAR · the trunk is a profile and not a taper · a root flare that dies off fast, a mid-height swell, and a power taper, each independently visible. Five segments, seeded wander per segment. 14 trees, 14 distinct bounding boxes, aspect ratios 0.59 to 1.32
- 15/08 · ENGINE · **depth in a scene is haze, not position** · the floating trees were caused by faking depth by raising far trees off the ground line, which a side-on scene cannot express. `Placement.recede` lerps a subject's whole palette toward the sky. Every tree now stands on the floor: 0 floating of 14. Declared cost: each haze band multiplies the wood's 25 colours again, 97 of 256 for three bands
- 15/08 · ENGINE · haze outranks the foot row in paint order · a hazier subject is behind a clearer one whatever their feet do; the foot row only separates subjects inside one band
- 15/08 · GRAMMAR · sway amplitude grows with branch level and the gust lags one level behind the next · the tree is in several states of the same gust at once. Per-tree gain on top: a heavy old tree moves less than a sapling
- 15/08 · LOOP · the golden ratio was the wrong spread for 14 wind phases · a tree's cycle is 16 frames, so two phases 0.034 apart land on the SAME frame and the two trees sway identically. Even spacing at 1/14 clears the frame grid; a stride of 5 permutes it so spatial neighbours are far apart in the gust
- 15/08 · GRAMMAR · a limb leaves the trunk at 68 degrees maximum, bounded in the function · at 2.1x a divergence of 0.135 put a limb at 102 degrees, which points down and out. It carried foliage below the trunk base, and the outline lock read that as the cell being too short — the wrong diagnosis of a real defect. Foliage below the trunk base is foliage underground once a scene stands the tree on a floor
- 15/08 · LOOP · the region check named three trees as noise rather than shading before he saw them · mean same-tone region 3.6 px on tree-snag against 6.8 for the idiom he ranked first, 62% of them single pixels. Foliage smaller than its own outline cannot make a region. Wood average now 6.5
- 15/08 · CLOSED 16/08 · ~~cloud continuity needs the live runtime~~ · delivered: motion is scene pixels per SECOND with a cadence, and there is no loop point left to be seamless at · drift wraps seamlessly only when a cloud crosses the whole scene in exactly one cycle, which at 1200 ms is a jet. Same piece as the gorilla's control: input, state, draw, driven by elapsed time
- 15/08 · GATE · his fourth reading: the diversity is "notável", and three things remain · one tree still off the ground, one trunk base far too fat, and the cloud and the actor are still owed. He also asked for the guard rails: "seria bom você ter essas referências para não cometer mais esse tipo de erro"
- 15/08 · ENGINE · **a tree places by `baseY`, which aligns its ORIGIN, not by `footY`, which aligns its lowest painted pixel** · a tree's lowest pixel is a root or the outline ring, 7 to 13 px below the trunk base, so five of fourteen stood that far in the air. `baseY` is exact and measures nothing
- 15/08 · LOOP · **the check that missed it asked whether `footY >= ground`, which is the number I had SET** · the trunk base is the number that matters. A check written against the parameter you just set can only confirm you set it. `tests/forest.test.ts` measures the base
- 15/08 · GRAMMAR · **a trunk has a physical ceiling and `makeTree` throws past it** · `tree-broad` shipped with a flared base 0.57 of the whole tree's height. A baobab is 0.25 and it is the stoutest tree there is. Bound at 0.17 of the trunk height as an argument, checked at 0.25 of the measured tree as a lock. It throws rather than clamps: a clamp quietly redraws what the author asked for and the author never learns
- 15/08 · GRAMMAR · a tree is made wide by its branches, never by its trunk · `tree-broad` keeps its width in `reach` and gives its girth back. And raising `height` to clear the ceiling stretched every limb by the same 57%, because `reach` is a fraction of `height` — two arguments, one number underneath
- 15/08 · ENGINE · **the composition moved into the browser and the drawing did not** · `src/scene/layers.ts` ships cropped sprites plus an arrangement; the page runs input, state and draw per animation frame. Sprites stay pre-rendered by the deterministic core, hashed and locked. This is the first piece of the engine slice and CONTENT asked for it
- 15/08 · ENGINE · the cloud's jump and the uncontrollable gorilla were one limitation, not two · composition done ahead of time. A pre-composited frame list is periodic by construction, so a cloud could only return to its start by crossing the whole scene in one 1200 ms cycle
- 15/08 · ENGINE · cloud motion is scene pixels per SECOND with a sway and a bob on its own period · 17, 23 and 29 seconds, deliberately not multiples: clouds on one period rise and fall together and the sky reads as one object with three parts. Same mistake the wood made with its wind
- 15/08 · ENGINE · **facing costs a second render, not a flip** · mirroring a sprite mirrors its lighting. The left-facing actor is rendered again with the lamp mirrored and then flipped, so the two mirrors cancel on the light and compose on the shape. Cheap here, impossible for a painted sheet
- 15/08 · HARNESS · **the game loop is locked headless against a fake DOM** · `tests/micro-runtime.test.ts` runs the page's own inlined script and asserts what an eye would check: a cloud may only jump while entirely off screen, the keys move the actor, the edges stop him, turning round selects the re-lit layer
- 15/08 · LOOP · **that lock found a latent defect in the compositor within minutes of existing** · `hsh ^= hsh >>> 13` yields a SIGNED 32-bit integer in JS, so `hsh % colors.length` returned -1 for about half the rain columns and indexed the palette entry before the rain's. Silent since the rain was written, because the wrong colour is a plausible grey
- 15/08 · ENGINE · the page is gzipped, and it is not a nicety · a stage ships every sprite's own frames instead of one flattened list. 2.07 MB of indexed art with wide transparent margins compresses to 286 KB, 7.2 to 1
- 15/08 · CLOSED 16/08 · ~~the actor holds one frame when still~~ · `gorilla-idle` and `astro-idle` are gaits now, and a released key gives an idle rather than a held frame. Locked · there is no idle animation, only a walk. A stand is a second gait and nobody has asked for one
- 15/08 · OPEN · the shelf ships a full stage per card · fine at one game, wrong at twenty. It becomes a still frame or a shared cache when the shelf grows
- 15/08 · GATE · his fifth reading: "ficou muito bom", and one thing he could not name · "tem algumas árvores que estão no plano de trás do gorila e a base da árvore está abaixo do gorila". Two defects at once, both in the engine
- 15/08 · GRAMMAR · **a trunk painted 10 px below where it said it stood** · the base capsule ended 2 px below the origin with a radius of 8, and a capsule has a round cap. Placing a tree by its origin placed it by a row nothing visible was on, and the wood's whole depth order is decided by that row. The endpoint moved to -0.62r and the overhang went from 12 px to 5
- 15/08 · GRAMMAR · a branch's ACCUMULATED angle is bounded, not each fork's · divergence, droop and jitter all add generation after generation, so tree-pine's limbs passed horizontal and grew downward. The 68 degree cap was applied before the jitter and then 25 degrees of jitter was added on top of it
- 15/08 · ENGINE · **`depth` is the only statement of distance, and row, haze and paint order all derive from it** · distance used to be stated twice, by `recede` and `baseY`, with nothing tying them. Two fields for one fact can disagree and did. The fix is not to check for the contradiction, it is to remove the ability to express it
- 15/08 · ENGINE · `anchor` says how a sprite meets its row: `origin` or `foot` · a tree's origin IS its ground contact, a gorilla's is his hips. The distinction cost two readings on its own
- 15/08 · ENGINE · a cloud is `sky`: no contact row, no ground haze, drawn first · thirty kilometres of air is not fifty metres of trees, and a cloud is behind the canopy because it is sky rather than because it is far. It was sorting between the middle and the front and could paint over a mid-band tree
- 15/08 · ENGINE · **the floor is a plane, not a line** · it was one flat band, so nothing told the eye that a lower row was nearer — which is why none of the depth work was legible. It now recedes by the same haze its trees carry, quantised to 8 steps
- 15/08 · ENGINE · **the gap between two depth planes must exceed what anything standing on them overhangs** · that is the general rule the defect was a case of. Overhang was 12 px against planes 11 px apart; it is now 5 px against planes 6 to 12 px apart, and a lock checks every pair
- 15/08 · LOOP · the first version of that lock compared every consecutive pair and failed on two trees in the same band · same depth is the same distance, so which of them overlaps the other is arbitrary and carries no claim. A depth invariant has to be stated BETWEEN depths. The lock was wrong, not the scene
- 15/08 · LOOP · a band is two planes, and the first attempt used three pixels · the lock caught two trees whose bases landed on the same row. The difference in what two trees overhang swallowed the difference in where they stood
- 15/08 · HARNESS · trunk diameter is read at a tenth of the tree's height, not at the base · at ground level the measurement is the ROOT FLARE, which is 1.5 to 2 times the trunk and varies with species far more. Diameter at breast height is what every published ratio is quoted against
- 15/08 · ENGINE · haze is quantised to four bands, and the cost is declared · every distinct haze value is a distinct copy of a subject's 25-colour palette. Seven planes cost 171 colours of 256 unquantised; four bands cost 146. Two planes sharing a band are separated by their row alone
- 16/08 · HARNESS · **every micro game carries a performance budget and a live meter, and they are different in kind** · the budget is counted from the stage and says what the page WILL ask for; the meter is measured in the browser and says what it DID. A budget that says cheap next to a meter that says 30 fps is a budget measuring the wrong thing. Neither alone is an instrument
- 16/08 · ENGINE · a rain drop is a stamp, not five fillRects · 240 canvas calls a frame against 19 for every subject in the wood put together, and a canvas call costs the same whether it moves one pixel or a thousand. 68 total now, and the stamp is byte-identical because x0 and the row index are both integers
- 16/08 · HARNESS · every ceiling in `tests/performance.test.ts` is derived from 16.67 ms and states its cost per unit · they bound what a page ASKS for, not any machine's speed. This suite runs in Node and cannot time a browser, and saying so is part of the instrument
- 16/08 · HARNESS · the determinism lock now exempts a CLOCK, and only a clock, in the two files that emit browser runtime code · Math.random, Date.now and new Date stay banned everywhere without exception. A clock that measures how long the drawing took cannot change what was drawn, and the determinism hash catches it if one ever does. The exemption list is itself asserted to name files that exist
- 16/08 · COMMISSION · **the forest gets a mechanic**: photographers enter from the edges, lie down, and shoot the gorilla; the gorilla attacks and they run home. Plus an idle. His words, and it is the first thing in this project a person can lose at
- 16/08 · GRAMMAR · **run 13, the photographer: three gaits on one body, and `prone` turns the spine ninety degrees** · run 7 tested "an action is data over a body" with a jump and an attack. This tests it at a quarter turn with the legs trailing behind, which is as far from the rest pose as a person gets
- 16/08 · GRAMMAR · one `gait.swing` serves three gaits, so the walk's keys are small on purpose · the amplitude is set by the LARGEST motion, which is the prone pose. The first draft reused a walk's usual key of 0.9 against a 104 degree amplitude and the legs came out in a 94 degree split
- 16/08 · GRAMMAR · the camera is a BONE off the near forearm, not a part on the arm · it has to sit at the eye in one pose and swing at the chest in another, and only a bone can be somewhere different in two gaits without a track of its own
- 16/08 · GRAMMAR · **an idle is not the absence of an animation** · a released key used to hold frame 0 of the walk, and a held frame reads as a crash. Three motions on periods that do not divide each other — breath on the cycle, look on a half, weight on a third — so the pose never repeats inside the loop
- 16/08 · ENGINE · **a subject has named clips and a state names one of them** · nothing about which animation plays has ever reached the sprites. The gorilla's idle/walk/attack and the photographer's walk/prone/run are six grammars over two bodies
- 16/08 · ENGINE · **a press is latched, not sampled** · a tap that begins and ends between two animation frames is invisible to a loop reading key state, and at 60 fps that is a 16 ms window a person hits regularly. Found by the headless lock, which taps between two ticks because that is the cheapest thing to write
- 16/08 · ENGINE · the attack holds its last frame instead of looping · a 600 ms clip at 60 fps loops back to the wind-up mid-swing and the blow appears to be thrown twice
- 16/08 · ENGINE · one render per distinct (grammar, tunables, haze, facing), and the cache key IS the layer id · two photographers are two behaviours over one set of sprites. A layer named after the first placement that asked for it is a layer that lies about the second
- 16/08 · SCENE · **every tree is behind the player now** · his call: "no primeiro plano o jogador precisará de visibilidade de todo o plano frontal". A game design decision that outranks the picture — the front sixteen rows are where the mechanic happens and a trunk standing in them is a trunk hiding a photographer
- 16/08 · SCENE · the standoff (46 px) is deliberately larger than the reach (40 px) · the photographer settles just outside the blow, so the player has to step in. A locked check, because a standoff inside the reach is a mechanic where nobody ever moves
- 16/08 · LOOP · comparing a character's x across a clip change measures the wrong thing · a different pose has a different painted box and therefore a different crop offset. That is the sprite changing, not the character moving, and it cost one false failure
- 16/08 · LOOP · **the page was blank in his browser and 136 locks were green** · `var off` inside the rain-stamp loop shadowed the offscreen buffer twelve lines above it — `var` is function-scoped — so every frame called drawImage on the number -1 and the browser threw on the first one
- 16/08 · HARNESS · **the real defect was in the instrument: a fake `drawImage` that accepts anything cannot report what a real one refuses** · the harness shrugged and reported green. The null-case rule (HARNESS.md section 5) applies to a test double exactly as it does to a measurement — an instrument has to be able to fail. The fake now type-checks its first argument, and the fix was verified by reintroducing the shadow and watching 10 locks go red
- 16/08 · HARNESS · a lock that just runs the loop for a minute and asserts it is still drawing · every other check in that file ticks a handful of frames for a specific answer, and none of them would have caught a page that never painted
- 16/08 · ENGINE · **the page reports its own boot state and catches `window.onerror` into a visible box** · I cannot look at his browser, so the page has to speak. A blank stage that says nothing costs a round trip to find out anything at all
- 16/08 · GATE · his fifth reading, three defects, **all three in the engine and none in the scene** · the photographer not lying on the ground, his legs inverted and his lens on his own face, and the gorilla shrinking during the attack. Locked in `tests/actor.test.ts` against every micro game, because a fix in the engine is a fix for every game on the shelf
- 16/08 · ENGINE · **`anchor: 'foot'` is applied per LAYER, not once per subject** · a photographer standing has his feet 24 px below his pelvis and lying down has them 5 px below it. Resolving the anchor once against the main clip drew him prone at standing height — 19 px in the air. The layer now carries its own `footOff` and whoever draws applies the anchor
- 16/08 · ENGINE · **every clip of a subject renders at ONE `body.scale`, and the subject decides it** · `gorilla-attack` was authored at 0.88 to sit on a sheet beside the jump. Nothing noticed until three clips became one character and the animal shrank a tenth every time it swung. A clip's tunables own its lighting and its timing; they do not own how big the body is
- 16/08 · LOOP · the scale lock reads what the engine RESOLVED, not what a tunables file declares · the declarations still disagree and are allowed to. A lock on the declaration would demand a fix in the wrong file
- 16/08 · GRAMMAR · **the camera is aimed, not carried: it has an angle track in every gait** · without one it inherits the forearm, and a forearm folded back to the eye points the lens at the photographer's own face. A wrist is the joint that separates where a hand IS from where it points
- 16/08 · GRAMMAR · the prone spine was at 52 degrees, which is a man kneeling · his head landed 8 px in front of his hips where a prone body puts it 20, and the knees folded 43 degrees backwards. The pose was fixed by SWEEPING four keys against landmark targets and reading the coordinates, never by looking
- 16/08 · GATE · **he felt the gorilla's elbow and knee before any number said anything** · "estou um pouco incomodado com o cotovelo e com seu joelho. O movimento está correto?" It was not. He asked a question rather than reporting a defect, and the question was right
- 16/08 · GRAMMAR · **a hinge bends to one side of straight and never through it** · the gorilla's knee spent seven frames of eight between -36 and +4 degrees, which is a shin pointing FORWARD of the thigh. The rest angle was -0.04 and is now +0.04, and the keys are authored as flexion in degrees: 10 at contact, 20 at load, 5 at pass, 40 through the swing
- 16/08 · GRAMMAR · the photographer's two elbows folded in OPPOSITE directions, inherited from probe D · on a side view both elbows point the same way. It is the same defect he saw in the prone legs, on the arms, and it had been in the tree since round zero
- 16/08 · HARNESS · **the hinge check runs over every grammar any micro game draws, in every gait** · take the child bone's angle relative to its parent across the cycle; if the range spans both signs by more than 3 degrees the joint bends both ways. Three degrees because a real joint hyperextends a little. It found four gaits at once
- 16/08 · GRAMMAR · **the silverback: the saddle is its own MATERIAL, not a lighter shift of the fur** · `shift` walks a part along its own ramp and cannot reach a colder hue, and grey hair is not pale fur. Two lobed masses rather than one band, because the silver runs over the shoulders and the loins and dips at the waist. 19% of the body
- 16/08 · GRAMMAR · the sagittal crest grew, and it is the half of the characterisation that changes the SILHOUETTE · a saddle is read in the fill, a crest in the outline, and a sprite is read at its outline first
- 16/08 · GRAMMAR · **a body and its palette are a pair: a new material obliges every palette that dresses that body** · the two run 8 ink probes re-palette the gorilla to compare inks, and adding `silver` broke both immediately. They inherit the obligation and should — a probe wearing a different animal than the one it is probing is no longer a comparison
- 16/08 · GATE · "cara o gorila é preto" · the fur ran to [182,164,140], a warm cream, which is a brown bear's highlight. **Black hair does not go warm in the light; it goes blue**, because the sky is the only thing it has to reflect. The ramp now spans luminance 9 to 134 and the animal's mean body luminance is 53 of 255
- 16/08 · GATE · "a costa prateada não é literal da forma como você fez" · the saddle topped out at [238,241,246] — eleven points off white — on an animal whose darkest tone was 14. That is a stripe painted onto a coat. **A silverback's saddle is the same coat going grey**, so its ramp now starts inside the fur's range and ends one step above its brightest tone
- 16/08 · IDIOM · **the player character was the only thing in the picture from a different game** · the wood, the clouds and the photographer were all in Chrono; the gorilla was still in the incumbent — eight tones over a narrow range, no drawn line, a rim instead, and 18 percent noise. Moved, with gorilla-idle, gorilla-attack, gorilla-jump and gorilla-mech
- 16/08 · IDIOM · **declared cost: Chrono's argument is a wide value range and a black animal cannot have one** · 9 to 134 here against 30 to 196 on the run 8 probe. The range is spent inside the dark half and the separation is bought with the drawn line and the saddle instead. A trade, not a win
- 16/08 · LOOP · **the repaint is the largest single measured gain of the session** · mean same-tone region on the gorilla went from 3.1 to 8.4 and single-pixel regions from 56% to 36%, against 6.8 and 40% for the sample he ranked first. Dropping 18% speckle and going from eight tones to five turned per-pixel noise into shading
- 16/08 · HARNESS · edge contrast against the FLOOR IT STANDS ON, not against the viewer's grey · a black animal on a dark forest floor is the case where a palette and its backdrop being one decision actually bites. 0.125 against the project's floor of 0.10
- 16/08 · GRAMMAR · a palette that borrows another one has to borrow its LENGTH too · the mech's steel and core indices were typed by hand — 17 through 24 — against a gorilla palette of two eight-tone ramps. Four five-tone ramps moved every one of them. They are derived now
- 16/08 · HARNESS · the "loud on the idiom he ranked last" null case moved from `gorilla-jump` to `probe-c` · the gorilla family went to Chrono, so the old sample stopped being an example of the thing the check exists to catch. A retired probe is a better calibration sample than a shipped subject, because it does not move
- 16/08 · GATE · "e esse buraco nas costas do gorila?" · nothing was transparent. It was the inner outline RINGING the silver saddle, and where a ragged marking's boundary folds back on itself the ring closes into a solid patch. Ink deep inside a body reads as a gap whatever colour the gap technically is
- 16/08 · GRAMMAR · **`Part.line` — a marking is not a solid and gets no inner outline** · the inner line exists to say "these are two things", which is right for a limb across a chest and wrong for a saddle, a blaze or a stripe. A marking is a region of the surface it lies on, and ringing it in ink is what turns grey hair into a painted badge. Skipped on BOTH sides of the test: a marking neither casts a line nor receives one
- 16/08 · GRAMMAR · defaults to true, which is the null case · every part authored before this renders byte-identical, and the baseline hashes say so
- 16/08 · LOOP · **the first draft of the hole check used a ceiling of 12 taken from one frame of one pose, and it failed every subject in the project including the one he ranked first** · a threshold read off a single sample measures that sample. Re-derived over the whole cycle against the ranked-first idiom
- 16/08 · HARNESS · the hole check is calibrated in both directions and the numbers separate cleanly · 83 px on `gorilla-jump-chrono`, 75 to 81 after the fix, 101 to 111 with the saddle ringed. The ceiling is 90, and reintroducing the ring turns three locks red
- 16/08 · HARNESS · the check is scoped to ACTORS, not to scenery · a tree of 97 branches carries hundreds of pixels of ink deep inside its outline and every one of them is a branch. A cloud does not outline in ink at all. The metric is about a body built from a few big masses
- 16/08 · GATE · **he had to report the same defect twice, and the first fix was real and was not it** · "você ainda não corrigiu... as costas do gorila possui um vão na região da cintura". Stopping the inner outline ringing the saddle was a true finding about a different thing. Recording it because the failure was mine: I measured what I had already guessed at instead of measuring what he pointed at
- 16/08 · LOOP · the second search found it by asking which PART owns each pixel along the back · at one column the owner was EMPTY — transparent canvas — with the outer outline traced round it. Neither the ink metric nor the flood-fill hole check could see it, because the notch opened onto the sky
- 16/08 · GRAMMAR · **`Part.marking` — a region of another surface, not a solid** · a saddle, a blaze, a stripe. The saddle's lobed boundary stood three pixels proud of the chest and hips it lay on, and where the two masses failed to meet the gap between them was a notch cut into the animal's back
- 16/08 · GRAMMAR · a marking paints only where a solid already claimed the pixel, so it adds nothing to a silhouette and cuts nothing out of one · and it gets no inner outline, and casts none. Markings paint after every solid, so a marking always has a body to lie on and never decides what the body's outline is
- 16/08 · HARNESS · **the lock compares SILHOUETTES, not pixels** · render the body, render it again with every marking deleted, and the set of painted pixels must be identical. That is what catches a notch opening onto the sky, which no flood fill can find. Removing the clip turns three locks red with 56 to 58 px of added silhouette
- 16/08 · GRAMMAR · `Part.marking` defaults to false, the null case, and every part authored before it renders byte-identical · it replaces `Part.line` from the same day, which was the same idea seen through one of its two consequences
- 16/08 · GATE · **his reading of `Part.marking` names a problem he had been sitting on since the beginning** · "você estava mantendo todas as formas geométricas que formam uma estrutura do corpo, com o contorno interno visível". The inner line was revealing the CONSTRUCTION of every body — which primitives it was assembled from — rather than its FORM. `marking` is the first tool in this project that lets a part exist for construction and not appear as a seam, and he expects it to matter for every future body
- 16/08 · GATE · **batch 1 delivered.** His sentence in one turn, no back-and-forth: an astronaut, jump and walk in all directions, lunar ground with the Earth behind. Prediction of a MISS recorded in `BACKLOG.md` before he looked, with the reason named
- 16/08 · ENGINE · **`src/core/yaw.ts` — a body turned about its own vertical axis, and it is a GRAMMAR TRANSFORM rather than a renderer change** · eight facings are eight grammars generated from one, which is the move `makeTree` made for a forest. Nothing in the rasterizer, the solver or the depth buffer learns a new idea
- 16/08 · ENGINE · the 2.5D vocabulary turns out to be exactly rotatable for the shape that matters · a capsule is a swept sphere: its endpoints are points and its radius is invariant under any rotation. An ellipsoid's silhouette radius is sqrt(rx²cos²+rz²sin²), also exact. That is why a limb survives being turned
- 16/08 · ENGINE · **a gait does NOT survive a yaw, and the decomposition has three parts** · limb rotation in the screen plane keeps cos(theta), becomes depth translation at sin(theta), and SHORTENS. The first version carried only the first two and the leap seen from behind measured the same bounding box as the idle — a jump nobody can see
- 16/08 · LOOP · the foreshortening is carried on `scaleY` at 0.6 of its true value, declared · `scaleY` multiplies down the bone chain, so a thigh and a shin that each shorten by 0.8 leave the shin at 0.64. Under-foreshortening reads as a stiff limb; over-foreshortening reads as a broken one
- 16/08 · ENGINE · **gravity, a height and a ground shadow — the first vertical motion in this engine** · a jumping body leaves its contact row and its shadow does not, and that shadow is the only thing telling a player where he lands. Without it a jump in an overhead view is a sprite drifting upward for no reason
- 16/08 · ENGINE · **the y-sort: a roaming player's paint order follows him** · every other subject has a fixed contact row decided when the stage is built. Walk south past a boulder and you have to come out in front of it, so the order is rebuilt each frame for the one subject whose row is state
- 16/08 · ENGINE · a diagonal is divided by sqrt(2), and the depth axis moves slower than the screen axis · the floor is foreshortened, so a step into the picture covers fewer rows than the same step across it. That single ratio is what makes a flat band read as a plane
- 16/08 · LOOP · **one key means different things to different actors, and the actor decides** · space swings the gorilla's fist and launches the astronaut. The attack branch was unguarded, so the moon's player entered a state whose clip is undefined and the draw looked up `undefined-e`
- 16/08 · SCENE · **the moon inverts two rules the forest taught, and both are knobs going to an extreme rather than mechanisms being removed** · `haze` is ZERO because there is no air, so distance is carried by the floor alone; and the sky is the darkest thing while the ground is the brightest, so every silhouette reads the other way up
- 16/08 · SCENE · shadow.strength is 2 ramp steps and fill.weight is 0.03, both against the project's 1 and 0.15 · no atmosphere means nothing scatters light into a shadow. The setting expressed as numbers rather than drawn
- 16/08 · SCENE · the Earth gets no drift, no bob and no cadence · the moon is tidally locked, so from a landing site it hangs in one place forever. Next to three clouds that never stopped moving in the forest, that stillness is the loudest thing in the scene
- 16/08 · GATE · **batch 1: MISS.** Hit rate 0/1. He shipped the environment outright — "superou minhas expectativas" on deep space and the Earth — and shipped the astronaut as a left/right game: "não teria defeito algum". The eight-way turn missed
- 16/08 · GATE · **the prediction was right and its reasoning was wrong, which is the more useful finding** · I predicted the yawed GAIT would fail and it did not — the back view keeps 65% of the side view's motion. What broke was `Part.z` never being yawed, the compass signs being inverted, and the arms being animated at nine degrees. **A declared risk is a place I was already looking**; the next prediction should name what I have NOT examined
- 16/08 · ENGINE · **`Part.z` is half of where a part is, and rotating only its `x` is half a rotation** · the visor sits on the front of the helmet at z -3.6 and stayed on the camera side in every facing. He walked north and saw the face
- 16/08 · ENGINE · a bone's REST angle decomposes under a yaw exactly as a gait key does · an arm hung 11 degrees out from the body is splayed forward and back, in the plane the author was looking at. Seen from the front that splay is depth, and an arm keeping its 11 degrees on screen is an arm sticking out sideways — "os braços estão tortos"
- 16/08 · GRAMMAR · **a fact about a subject is not a licence to stop animating it** · Apollo crews loped with their arms out for balance rather than swinging them, which is true, and I applied it until the range was nine degrees. Twenty-six is restrained; nine is a still image
- 16/08 · GRAMMAR · both arms hang the same way and differ only in PHASE — the third time this project has shipped mirrored elbows, after probe D and the photographer
- 16/08 · ENGINE · **the floor walks its own ramp by depth, and haze rides on top** · it used to be one tone diluted by haze, and the moon's haze is zero — so the whole plane was a single value. "Me parece apenas um chão preto com pedras". Haze was doing two jobs and only one of them was about air
- 16/08 · SCENE · dust is a backdrop, like the stars, because it does not move · regolith is powder churned by four billion years of impacts, and a flat fill reads as a floor tile however carefully its value is graded
- 16/08 · SCENE · a crater's rim and shadow now agree with the lamp · the sun is upper-left, so a depression pools shadow on its upper-left inner wall. The first version put it lower-right, which is the lit side, so every crater disagreed with the Earth above it about where the sun was
- 16/08 · HARNESS · **the hole check became a DIFFERENCE and the absolute ceiling was withdrawn** · 90 px was calibrated on the gorilla and then failed the astronaut, whose PLSS pack is a hard-edged box that SHOULD be outlined. A threshold derived from one body measures that body
- 16/08 · HARNESS · **and the new check does not catch the original defect, which is stated rather than hidden** · delete `marking: true` from the saddle and it goes green, because the saddle is then a solid present in both renders. `marking` is a declaration and no measurement can infer intent. What the locks guarantee is that a DECLARED marking behaves; whether it should have been declared is his eye
- 16/08 · GATE · his reading of the eight facings, and the platform half is DONE: "a movimento A,D, as sombras dessa perspectiva, as animações dos braços, do pulo, o visor... tudo perfeitamente ok". Five separate failures named in the other seven
- 16/08 · ENGINE · **`Part.z` was being used as a paint-order device, and in a side-view-only world nobody can tell** · the visor was authored at cx 2.2 with z -3.6, half of it forward and half of it shoved at the camera to clear the skull. The shove is a POSITION, so it rotated: a quarter turn south sent the visor 3.6 px to the left. **The moment a body can turn, every depth lie becomes a visible defect**
- 16/08 · ENGINE · **a marking is a decal: it wins the depth test against the surface it lies on, and is not drawn at all when its own depth puts it on the far side** · that is what removed the need to shove a marking at the camera, and therefore what made a turnable body possible. A visor authored on the front of a helmet now disappears round the back on its own
- 16/08 · ENGINE · the pack carried `z: 5.6` for the same reason and it put the pack five pixels to one side, on top of an arm · a pack is centred across a person's back: behind them in x and nowhere in z. Its depth comes from its own `d`, which is what a solid's depth is for
- 16/08 · LOOP · **the yaw's depth companion was six times too strong, and it was a unit error wearing a plausible number** · it read `key x sin(theta) x 10` and the runtime then multiplied by `gait.depth`. A key of -0.22 became ELEVEN pixels of depth travel on a body whose limb rows are 6.6 apart. Every limb in every turned facing was flung through the torso — "quando ando SD, eu nem consigo descrever o que está errado"
- 16/08 · LOOP · a track key is a fraction of an amplitude, so turning one into a distance needs the amplitude it is a fraction of, the radius the rotation acts at, and the amplitude the answer is read back through · leaving any of the three out is the same class of bug
- 16/08 · GRAMMAR · the arms went from ±5 to ±7.6 and the forearm gained an outboard splay · a pressure suit holds the arms out, and in a side-authored body the only axis for saying that is depth. Set by the facing he could NOT read rather than the one he could
- 16/08 · HARNESS · **every facing keeps every limb, and nothing had ever asked that per facing** · the findings channel has asked "is a part absent" since round zero. Until this morning a body had one facing. The far arm behind the body in a pure side view is the only exemption
- 16/08 · LOOP · the yaw test asserted that a turned body gets NARROWER and that is simply wrong about bodies · a person is wider from the front than from the side, and the astronaut proved it the moment his arms went where a suit holds them. The test failed on a fix. What a turn must do is change the silhouette without collapsing it
- 16/08 · SCENE · six craters and two rocks, at 0.66 of a circle rather than 0.42 · "o solo lunar é mais memorável por suas crateras". A boulder on the moon is a rare thing; a crater is what the ground is made of
- 16/08 · GATE · "os antebraços parecem estar desconectados dos braços" · a 2.6 px gap at the elbow, and it was there because I held the forearm outboard with a DEPTH offset. A pressure suit does hold its arms out and that is a true fact; expressing it as a translation at the elbow is a broken joint
- 16/08 · ENGINE · **anything authored purely in depth is unfalsifiable in the view it was authored in** · that is the general form of the last three defects — the visor's shove, the pack's shove, the forearm's splay. All three were invisible in the side view and all three were the first thing he saw when the body turned. A body that can turn has no hiding place
- 16/08 · HARNESS · **a limb's segments must touch, in every facing and every frame** · the check computes where a parent bone's own length ends and asks how far the child starts from it. It is the cheapest possible geometric truth about a body and nothing had ever asserted it, because in one view a broken joint can be hidden behind the parent
- 16/08 · LOOP · the width-ratio floor in the yaw test is 12% and it is honest about why · a pressure suit is a stack of near-cylinders, which is why it survives being turned AND why its facings differ less than any other body's would. Asserting a gorilla's ratio there would be asserting a fact about a subject the file has never seen
- 16/08 · GATE · he diagnosed both remaining defects himself, and both were one bug · "o antebraço entra no corpo do astronauta" and "parece que o antebraço está atrás do braço"
- 16/08 · ENGINE · **a bone's rotation moves its CHILDREN in depth, never itself** · the yaw folded a bone's rest angle and its gait keys into that bone's own `z`. A joint's origin does not move when the joint turns; what hangs off it does. The error stacked down the chain, so the forearm sat two units further from the camera than the elbow it hangs from
- 16/08 · ENGINE · the depth track is emitted per CHILD, at that child's own reach · rotating a shoulder moves the elbow by the length of the upper arm and the wrist by that plus its own. One radius for every bone was the same unit error as the `LIMB = 10` before it, one level up
- 16/08 · ENGINE · **declared approximation the transform cannot avoid** · the PARTS on a rotating bone are not tilted into depth, because a capsule carries one depth for its whole length. A limb swung at the camera moves its joints correctly and keeps its own segments flat. Under two pixels at walking amplitudes; a limb thrown straight at the viewer would show it
- 16/08 · GRAMMAR · **the number that mattered was the FOREARM's clearance, not the shoulder's** · an arm leaning back swings its wrist toward the body's centre line, and at three quarters that drift ate the whole clearance. The lean came down as the shoulder row went out: ±8.4 and a 10 degree hang against ±5 and 23 degrees
- 16/08 · SCENE · every rock is small now, at his instruction · a boulder on a plain competes with the figure for the eye; a scatter of stones is texture, and the craters do the work of saying where you are
- 16/08 · GATE · he asked twice whether any of this reaches past the astronaut, and the only honest answer is a measurement · so the transfer was TESTED rather than claimed: the photographer and the gorilla both predate `yaw.ts` and neither was authored to turn. Both turn, with every joint holding at 0.00 px
- 16/08 · HARNESS · **the transfer is a lock now, not a claim** · `tests/yaw.test.ts` turns two bodies that were drawn for a side-scrolling wood and asserts they change silhouette, do not collapse, and keep every joint. If a future change to the transform only works on the body it was written for, that lock goes red
- 16/08 · ENGINE · **what transfers is the MECHANISM; what does not is the TUNING, and that is a cost per subject rather than a defect** · how far a body's shoulders sit from its chest decides whether an arm survives being turned, and no transform can invent a proportion its author did not give it. What the locks do is tell you: a body with no depth on its limb rows fails immediately
- 16/08 · SCENE · the craters came down to a third of their radius, at his instruction · a crater wide enough to hold the player is a crater he walks inside rather than past. The largest is now 44 px across against a 46 px figure
- 16/08 · MEASURED · **where the 2.5D yaw breaks, measured across every body in the project** · a body's depth over its width decides it. mantis 0.20 collapses 46 px to 10; scorpion 0.33 to 12; beetle 0.42 to 16; gorilla 0.46 holds at 27; photographer 0.46 holds at 25. **The threshold is about 0.45** and it is a fact about the SUBJECT, not about the transform — a mantis seen head-on really is a sliver
- 16/08 · OPEN · **every defect he has found in the turn was my bug, not the approximation's limit** · the depth lies, the LIMB unit error, the bone-versus-child fold, the elbow translation. Four defects, four implementation errors of a sound approximation. There is not yet one case that traces to the model itself, and that is the evidence a decision about 3D should wait for
- 16/08 · OPEN · **what the 2.5D vocabulary cannot express AT ALL is pitch and roll** · a bone has one angle and it is in the screen plane. A body leaning into a turn, a ship banking, a figure tumbling as it falls — none of those is a tuning problem, they are inexpressible. That is a larger gap than facing was
- 16/08 · OPEN · **`compose.ts` and `layers.ts` are two independent code paths drawing the same scene, and nothing compares them** · the gallery freezes and the shelf is live, which is why they are separate. They agree today only because they share the depth functions. **This matters most to whoever changes the core next**: a 3D skeleton touches both, and a divergence between them would show as "the gallery looks different from the game" with no lock to say why
- 16/08 · OPEN · a 4 px enclosed pocket between the gorilla's legs in one frame of eight, and 1 px in the attack · small, visible for 75 ms, and named rather than pretended away
- 16/08 · OPEN · **the forest has a mechanic and no consequence** · a photographer shoots forever, the gorilla drives him off forever, and nothing is ever won or lost. It is the smallest work on the list with the largest return, and it is what turns a reactive scene into a game
- 16/08 · OPEN · an attack that lands and one that misses look identical · no impact feedback of any kind
- 16/08 · GATE · **batch 2 given, and it is the first commission that is not about drawing** · *"um jogo de plataforma em que um gatinho pula de plataforma em plataforma... com uma estética de Cozy Game"*, and the sentence before it chose the round: *"não vou atacar o 3d agora pois segundo sua própria recomendação precisamos atacar a consequência primeiro."* He read the OPEN line about the forest having a mechanic and no consequence, and picked it over the 3D work
- 16/08 · ENGINE · **the first game on the shelf that can be LOST** · a camera that only rises, a tower with no top, and a fall past the bottom of the screen that ends the run and names the height it ended at. Everything before this reacted; nothing before this could be won or lost
- 16/08 · ENGINE · **a climb's tower is generated, never stored, by the same integer hash the stars and the rain use** · band `k` yields its shelves closed-form, so an unbounded world costs no memory and replays identically on every machine. That is `CLAUDE.md` §1's rule — a recorded input sequence must replay to the same state — reaching the level of the world rather than the sprite
- 16/08 · ENGINE · **the tower is provably climbable and the proof is one inequality** · apex 78 px against a worst gap of 46, asserted over ten thousand bands rather than argued. A level designer is what this replaces
- 16/08 · LOOP · **the gap bound was `bandH` and it is `bandH + jitterY`, because jitter compounds across a PAIR** · a shelf may sit below its band's row, which shortens the gap above it and lengthens the gap below it by the same amount. Claimed 52, measured 67. The claim was checkable and I wrote it as reasoning instead
- 16/08 · LOOP · **a cliff instead of a curve is the shape of a geometric lock, and I swept a difficulty knob four times before reading it that way** · the landing window at 8, 14, 20 and 26 px all gave exactly 4.0 m, then 34 gave 10. A difficulty setting produces a gradient. **Four identical results ARE the finding, and I treated them as four data points on a curve I had not looked at.** portable
- 16/08 · ENGINE · **with one shelf per band and a steady input, the game is a periodic system and it locked** · the crossing position at every shelf above a landing is a fixed offset from it, so either the offset matches or it never does. The run bounced between the garden and the first shelf for ever. Spacing the shelves round the world by CONSTRUCTION is the fix; slicing the width into ranges is not, because two shelves either side of a boundary sit next to each other
- 16/08 · HARNESS · **the runtime returns its own state, and it is a findings channel rather than a picture** · a scrolling game cannot be diagnosed from its draw calls, because a screen row is a world row minus a camera and neither is recoverable from the difference. It found the locked orbit that four tuning sweeps had failed to explain
- 16/08 · HARNESS · **the budget did not know about a whole draw path and reported 3 calls a frame for a frame that made 237** · an instrument blind to a code path reports cheap, which is the flattering direction §5 says instrument defects always come in. Taught it the climb, then baked the star field and the floor into stamps: 98 calls
- 16/08 · GRAMMAR · **a phase offset is not a separation** · two limbs at the same angle occupy the same pixels whenever the phase lines up. The kitten's first draft put both front legs inside a ten-degree band and the far one painted 2 to 4 px in every frame of eight. Fourth occurrence of the class, after probe D, the photographer and the astronaut
- 16/08 · HARNESS · **the fix for that class was written as a pixel check, failed its calibration, and was withdrawn before shipping** · "declared shape area against best painted frame" fired on 237 parts across 69 subjects, including bodies he passed without a word. **Occlusion is normal**, and no pixel count separates a part correctly hidden from one accidentally hidden, because the difference is intent
- 16/08 · HARNESS · **the invariant was in the wrong place: a limb pair being one pose is a fact about the GRAMMAR, not about the render** · `tests/pairs.test.ts` samples both bones of a mirrored pair per frame and asserts they separate somewhere in the cycle. It fired on nine of sixty-nine subjects and **all nine are symmetric by intent** — a landing, a leap, an idle, a body lying down. It fired on no walk, no lope, no run
- 16/08 · LOOP · so the rule the four defects were about is narrower than it looked: **a locomotion cycle must alternate; an action need not.** The nine exemptions are declared by name with their reason, the same way a marking is declared — no measurement can infer intent
- 16/08 · GRAMMAR · **the kitten's read is one ratio: the head is as wide as the chest** · an adult cat's skull is half that against the same body. It is 22 parts in a body 34 px tall, against a recorded ceiling of 28 in 30×28
- 16/08 · GRAMMAR · **"cozy" is a hue policy and it is not allowed to be a value policy** · the 15/08 verdict selected a wide value range and put the soft flat control last. The coat runs 36 to 205 in luminance and the ink is a warm brown instead of a black. Warmth is hue; softness would have been value, and this round did not spend it
- 16/08 · GRAMMAR · **an amplitude below one pixel of travel is a still image, and the perch's first sway was three degrees** · keys of 0.16 against a swing of 0.06 turns multiplies two kinds of restraint together and arrives at nothing: eight byte-identical frames, minimum pair distance exactly zero. The keys reach ±1 and the restraint lives in the amplitude, which is what the split between a normalized track and a tunable is FOR
- 16/08 · GRAMMAR · **two solids at equal depth do not compose, they contend** · the white tail tip laid over the coat at the same depth paid out between zero and three pixels. The fix is geometric — the coat stops short and the cream owns the rest — never a depth nudge
- 16/08 · GRAMMAR · a stem lengthened to clear the moss it was buried in is a stem that no longer fits the cell · the absence fix and the canvas resize belong in the same turn, and the silhouette lock is what connects them
- 16/08 · ENGINE · **a squash channel changes a sprite's bounding box, so a cell is sized for its widest FRAME and not for its rest pose** · the tuck's scaleX of 1.17 pushed the head 6 px past a cell that held every other clip
- 16/08 · SCENE · `compose()` REFUSES a climbing scene rather than rendering one · a scrolling, generated, stateful world has no frozen frame list, and a gallery entry that disagreed with the game would have no lock able to say why. That is the honest handling of the two-draw-paths divergence named this morning
